# Documentation index

- `design.md`: the subscription lifecycle, why server-side dependency
  tracking replaced the client-side entity scan, light versus shadow DOM, and
  why `always_update` is retired.
- `security.md`: threat model, what the sanitiser profile removes and keeps,
  and what it cannot prevent.
- `operations.md`: every configuration key and its consequence, the resource
  switch procedure from the upstream install, and troubleshooting.
- `decisions.md`: dated decisions with the alternative rejected and why.
- `quality-scale.md`: the repository's status against a Bronze to Platinum
  scale adapted for dashboard cards.
- `backlog.md`: deferred work and open verification items.
